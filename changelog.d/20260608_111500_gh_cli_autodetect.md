### Added
- Automatically detect the GitHub token and target repository from the `gh` CLI when explicit GitHub inputs are not provided.
- Execute GitHub CLI detection through the `command-stream` crate.
